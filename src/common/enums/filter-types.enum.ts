export enum FilterTypes {
	UUID = 'uuid',
	NUMBER = 'number',
	STRING = 'string',
	DATE = 'date',
	BOOLEAN = 'boolean',
	ENUM = 'enum',
	ARRAY_OF_ENUM = '[enum]',
	ARRAY_OF_NUMBERS = '[number]',
	ARRAY_OF_STRINGS = '[string]',
	ARRAY_OF_DATES = '[date]',
}
